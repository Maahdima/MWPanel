version: "2"
sql:
  - engine: "sqlite"
    queries: "query.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        overrides:
          - db_type: "TEXT"
            nullable: true
            go_type:
              pointer: true
              type: "string"